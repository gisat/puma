#!/usr/bin/env node

const PasswordHash = require('../security/PasswordHash');

if (process.argv.length !== 3) {
    throw new Error('Script expects exactly one parameter - password.');
}

new PasswordHash(process.argv[2]).toString().then(console.log);
