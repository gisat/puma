// Script to be run on NodeJS environment. The input is the bounding box. The result is printed in the console and therefore
// could be simply redirected to any output.

let superagent = require('superagent');
let osmtogeojson = require('osmtogeojson');

