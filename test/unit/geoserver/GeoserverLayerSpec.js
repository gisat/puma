var should = require("should");

describe('GeoserverLayer', function(){

	before(function(){

	});

	it('It is possible to update existing layer in the geoserver', function(){

	});

	after(function(){

	});
});